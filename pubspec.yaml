name: chatwhiz
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  get:
  get_storage:
  dynamic_color:
  url_launcher:
  dio:
  flutter_colorpicker: 
  flutter_markdown: 
  flutter_i18n: 
  cupertino_icons: ^1.0.8
  bitsdojo_window: 
  fluent_ui: 

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/
    - assets/images/
    - assets/locales/

